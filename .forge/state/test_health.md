# Test Health

Latest known result:

```text
python -m pytest
10 passed
```

`forge verify` also runs:

```text
python -m pytest
python -m compileall -q src/forge
```

Latest successful run:

```text
20260508-193653
```

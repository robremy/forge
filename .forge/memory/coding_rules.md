# Coding Rules

- Prefer simple Python.
- Keep modules small.
- Use explicit file paths.
- Handle missing markdown files gracefully.
- Add tests for every behavior change.
- Avoid global mutable state.
- Do not let model output determine shell commands.
- Keep run artifacts readable.
